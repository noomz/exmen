---
phase: 07.1-ui-improvements
plan: 01
type: execute
depends_on: []
files_modified: [Exmen/Views/ActionRowView.swift, Exmen/Views/MenuContentView.swift, Exmen/Views/PopupResultView.swift]
---

<objective>
Make action list more compact to show more items, and improve popup to show full content.

Purpose: Better usability - users can see more actions at once and view full script output.
Output: Compact action rows showing 8+ items, popup with larger content area.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Exmen/Views/ActionRowView.swift
@Exmen/Views/MenuContentView.swift
@Exmen/Views/PopupResultView.swift

**Current issues (from screenshots):**
- Action rows: padding .vertical(8) + .horizontal(12), spacing 4 between rows
- Menu height: 320px fixed, showing only ~4 items
- Each row shows description which takes vertical space
- Popup: 400x300 fixed, header/footer consume space, content area limited

**Target improvements:**
- Reduce row padding to show 8+ actions
- Hide description by default, show on hover
- Increase menu height to 400px
- Popup: larger content area, smaller header/footer padding
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact ActionRowView</name>
  <files>Exmen/Views/ActionRowView.swift</files>
  <action>
  Make action rows more compact:

  1. Reduce vertical padding from 8 to 4
  2. Hide description by default, only show on hover (or if it's a status)
  3. Make icon smaller (frame width 16 instead of 20)
  4. Reduce HStack spacing from 12 to 8

  Keep the hover background effect and play icon behavior.
  Status text (dynamicStatus) should still show since it's dynamic info.
  </action>
  <verify>Build succeeds: `xcodebuild -scheme Exmen build`</verify>
  <done>Action rows are visually more compact with description shown on hover only</done>
</task>

<task type="auto">
  <name>Task 2: Increase menu height and reduce spacing</name>
  <files>Exmen/Views/MenuContentView.swift</files>
  <action>
  Increase visible action count:

  1. Change menu frame height from 320 to 400 (when not showing popup)
  2. Reduce LazyVStack spacing from 4 to 2
  3. Reduce vertical padding in ScrollView from 8 to 4
  4. Keep header/footer padding as-is (they're already reasonable)
  </action>
  <verify>Build succeeds: `xcodebuild -scheme Exmen build`</verify>
  <done>Menu shows 8+ action items, scrolls smoothly</done>
</task>

<task type="auto">
  <name>Task 3: Improve PopupResultView layout</name>
  <files>Exmen/Views/PopupResultView.swift</files>
  <action>
  Maximize content area in popup:

  1. Increase popup size from 400x300 to 450x400
  2. Reduce header padding from .padding() to .padding(12)
  3. Reduce content ScrollView padding from .padding() to .padding(12)
  4. Keep footer padding at 12 (already compact)
  5. Use smaller font for exit code/duration (.caption2 instead of .caption)

  The goal is more vertical space for script output/errors.
  </action>
  <verify>Build succeeds: `xcodebuild -scheme Exmen build`</verify>
  <done>Popup shows more content, less wasted space</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -scheme Exmen build` succeeds
- [ ] Run app and verify action list shows 8+ items
- [ ] Verify hover shows description on action rows
- [ ] Verify popup has more content space visible
</verification>

<success_criteria>

- All tasks completed
- Action list shows 8+ items in default height
- Description shows on hover, status always visible
- Popup content area is larger
- No visual regressions or layout issues
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ui-improvements/07.1-01-SUMMARY.md`
</output>
