---
phase: 08-long-running-cli-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Exmen.xcodeproj/project.pbxproj
  - Exmen/Models/ActionConfig.swift
  - Exmen/Models/Action.swift
  - Exmen/Models/ServiceConfig.swift
  - Exmen/Models/ServiceState.swift
  - Exmen/Services/ManagedService.swift
  - Exmen/Services/ConfigLoader.swift
autonomous: true
requirements:
  - SVC-MODEL
  - SVC-CONFIG

must_haves:
  truths:
    - "Service TOML configs with type = service and [service] section parse correctly"
    - "Existing action TOML configs continue to load without changes (backward compatible)"
    - "ServiceState enum exists with stopped/starting/running/restarting/crashed states"
    - "ManagedService class skeleton exists with published state properties"
  artifacts:
    - path: "Exmen/Models/ServiceConfig.swift"
      provides: "ServiceConfig, RestartPolicy, ActionType types"
      contains: "enum RestartPolicy"
    - path: "Exmen/Models/ServiceState.swift"
      provides: "ServiceState enum with color and display helpers"
      contains: "enum ServiceState"
    - path: "Exmen/Services/ManagedService.swift"
      provides: "ManagedService ObservableObject skeleton"
      contains: "class ManagedService"
    - path: "Exmen/Models/ActionConfig.swift"
      provides: "Extended ActionConfig with type and service fields"
      contains: "ActionType"
  key_links:
    - from: "Exmen/Models/ActionConfig.swift"
      to: "Exmen/Models/ServiceConfig.swift"
      via: "service: ServiceConfig? property"
      pattern: "let service: ServiceConfig"
    - from: "Exmen/Models/Action.swift"
      to: "Exmen/Models/ServiceConfig.swift"
      via: "serviceConfig property"
      pattern: "let serviceConfig: ServiceConfig"
---

<objective>
Add SwiftTerm SPM dependency, extend config models for service type, and create all type contracts needed by downstream plans.

Purpose: Establish the data model foundation so Plan 02 (ServiceManager) and Plan 03 (UI + wiring) can build against defined contracts without ambiguity.
Output: SwiftTerm importable, ActionConfig parses service TOMLs, ServiceState/ServiceConfig/ManagedService types exist.
</objective>

<execution_context>
@/Users/noomz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/noomz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-new-feature-to-support-long-life-arbitrary-cli-programs-as-service/08-CONTEXT.md
@.planning/phases/08-new-feature-to-support-long-life-arbitrary-cli-programs-as-service/08-RESEARCH.md

@Exmen/Models/ActionConfig.swift
@Exmen/Models/Action.swift
@Exmen/Services/ConfigLoader.swift
@Exmen.xcodeproj/project.pbxproj

<interfaces>
<!-- Current ActionConfig (will be extended) -->
From Exmen/Models/ActionConfig.swift:
```swift
struct ActionConfig: Codable {
    let name: String
    let icon: String?
    let description: String?
    let script: ScriptConfig
    let output: OutputConfig?
    let hook: HookConfig?
    let hide_on_click: Bool?
}
```

From Exmen/Models/Action.swift:
```swift
struct Action: Identifiable {
    let id: UUID
    let name: String
    let icon: String
    let description: String
    let scriptConfig: ScriptConfig?
    let outputConfig: OutputConfig
    let hookConfig: HookConfig?
    let hideOnClick: Bool
    // Dynamic state...
    init(from config: ActionConfig) { ... }
}
```

<!-- TOML structure (from existing configs): -->
<!-- Top-level: name, icon, description -->
<!-- Sections: [script], [output], [hook] -->
<!-- New: type field at top level, [service] section -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SwiftTerm SPM dependency and create service type contracts</name>
  <files>Exmen.xcodeproj/project.pbxproj, Exmen/Models/ServiceConfig.swift, Exmen/Models/ServiceState.swift</files>
  <action>
1. Add SwiftTerm as an SPM dependency to the Xcode project. Use `xcodebuild -resolvePackageDependencies` after modifying the pbxproj file. The package URL is `https://github.com/migueldeicaza/SwiftTerm` with minimum version 1.11.2 (up to next major). Follow the exact pattern used for the existing TOMLDecoder dependency in pbxproj — add XCRemoteSwiftPackageReference, XCSwiftPackageProductDependency, and link to the Exmen target. Use unique hex IDs that don't collide with existing ones.

2. Create `Exmen/Models/ServiceConfig.swift` with:
   - `enum ActionType: String, Codable` with cases `action` (default) and `service`
   - `enum RestartPolicy: String, Codable` with cases `never`, `onFailure` (coding key: `on-failure`), `always`
   - `struct ServiceConfig: Codable` with fields:
     - `command: String` (required — the executable to run)
     - `args: [String]?` (optional arguments)
     - `restart: RestartPolicy?` (default: .never)
     - `max_restarts: Int?` (default: 3)
     - `keep_alive: Bool?` (default: false)
     - `working_dir: String?` (expands tilde)
     - `env: [String: String]?` (additional env vars)
   - Add resolved computed properties: `resolvedRestart`, `resolvedMaxRestarts`, `resolvedKeepAlive`
   - Add `resolvedWorkingDir` that expands tilde using NSString.expandingTildeInPath
   - Add `resolvedEnvironment` that merges ProcessInfo.processInfo.environment with PATH including /opt/homebrew/bin, sets TERM=xterm-256color, then overlays service-specific env vars

3. Create `Exmen/Models/ServiceState.swift` with:
   - `enum ServiceState: Equatable` with cases: `stopped`, `starting`, `running`, `restarting`, `crashed`
   - Computed property `dotColor: Color` — green=running, gray=stopped, yellow=starting/restarting, red=crashed
   - Computed property `isActive: Bool` — true for running, starting, restarting
   - Static function `displayText(state:startedAt:)` returning formatted string like "running - uptime 2h 15m" or "stopped" or "crashed". Use DateComponentsFormatter with .abbreviated style, maximumUnitCount 2, allowedUnits [.day, .hour, .minute].

Note: RestartPolicy needs custom CodingKeys because TOML uses `on-failure` (hyphenated) but Swift enum cases use camelCase.
  </action>
  <verify>
    <automated>cd /Users/noomz/Projects/Opensources/exmen && xcodebuild -scheme Exmen -configuration Debug build 2>&1 | grep -E "error:|BUILD"</automated>
  </verify>
  <done>SwiftTerm package resolves and links. ServiceConfig.swift and ServiceState.swift compile. RestartPolicy decodes "on-failure" correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ActionConfig and Action for service support + ManagedService skeleton</name>
  <files>Exmen/Models/ActionConfig.swift, Exmen/Models/Action.swift, Exmen/Services/ManagedService.swift, Exmen/Services/ConfigLoader.swift</files>
  <action>
1. Extend `ActionConfig` in ActionConfig.swift:
   - Add `let type: ActionType?` field (nil defaults to .action for backward compat)
   - Add `let service: ServiceConfig?` field (only present when type == service)
   - Add computed `var resolvedType: ActionType { type ?? .action }`
   - Add computed `var isService: Bool { resolvedType == .service }`
   - Make `script` optional (`let script: ScriptConfig?`) since services use `[service].command` instead of `[script]`. Update `resolvedOutput` and other computed properties to handle this gracefully.

2. Extend `Action` in Action.swift:
   - Add `let serviceConfig: ServiceConfig?` property
   - Add `let isService: Bool` property
   - Update `init(from config: ActionConfig)` to populate serviceConfig from config.service and isService from config.isService
   - Update the memberwise init to include `serviceConfig: ServiceConfig? = nil, isService: Bool = false`

3. Create `Exmen/Services/ManagedService.swift` as skeleton (implementation in Plan 02):
   ```swift
   import Foundation
   import SwiftUI

   @MainActor
   class ManagedService: ObservableObject, Identifiable {
       let id: UUID
       let action: Action

       @Published var state: ServiceState = .stopped
       @Published var pid: Int32?
       @Published var startedAt: Date?

       var restartCount = 0

       init(action: Action) {
           self.id = action.id
           self.action = action
       }
   }
   ```
   This is deliberately minimal — Plan 02 adds SwiftTerm integration, start/stop/restart logic, and output window.

4. Update `ConfigLoader.swift` to handle the fact that service-type configs have optional `[script]` section. The existing `loadConfig` and `loadAllConfigs` methods should continue working since we made `script` optional in ActionConfig. No other changes needed — ActionService in Plan 03 handles the filtering.
  </action>
  <verify>
    <automated>cd /Users/noomz/Projects/Opensources/exmen && xcodebuild -scheme Exmen -configuration Debug build 2>&1 | grep -E "error:|BUILD"</automated>
  </verify>
  <done>ActionConfig parses both existing action TOMLs (backward compatible) and new service TOMLs with type="service" and [service] section. Action model includes serviceConfig. ManagedService skeleton compiles with SwiftUI import. Build succeeds with zero errors.</done>
</task>

</tasks>

<verification>
1. Project builds successfully: `xcodebuild -scheme Exmen -configuration Debug build`
2. Existing action configs still load (no regression) — launch app, verify action list appears
3. SwiftTerm is importable: ManagedService.swift imports Foundation/SwiftUI, Plan 02 will add `import SwiftTerm`
</verification>

<success_criteria>
- SwiftTerm SPM dependency added and resolves
- ServiceConfig, ServiceState, RestartPolicy, ActionType types exist and compile
- ActionConfig extended with optional type + service fields
- Action extended with serviceConfig + isService
- ManagedService skeleton exists as ObservableObject
- All existing TOML configs still parse (backward compatible)
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-feature-to-support-long-life-arbitrary-cli-programs-as-service/08-01-SUMMARY.md`
</output>
