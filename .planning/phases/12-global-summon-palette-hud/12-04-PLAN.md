---
phase: 12-global-summon-palette-hud
plan: 04
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - Exmen/Models/HUDPresentationState.swift
  - Exmen/Views/ExecutionHUDView.swift
  - Exmen/Views/ExecutionHUDWindow.swift
  - Exmen/ExmenApp.swift
  - Tests/ExmenTests/HUDPresentationStateTests.swift
  - Tests/ExmenTests/SummonIntegrationTests.swift
  - Exmen.xcodeproj/project.pbxproj
autonomous: false
requirements: [SUMMON-01, SUMMON-02, SUMMON-03, SUMMON-04, SUMMON-05]
must_haves:
  truths:
    - "Starting an action from either menu or palette shows the same non-activating on-screen HUD"
    - "Scripts show indeterminate running state; orchestrations show live determinate overall progress and useful subtask detail"
    - "Success/failure remains visible briefly, then the HUD hides automatically without stealing keyboard focus"
    - "A stale completion timer can never hide the HUD for a newer execution"
    - "Global shortcut, Settings recorder, palette keyboard flow, execution, and HUD work together in a signed Debug app smoke test"
  artifacts:
    - path: "Exmen/Models/HUDPresentationState.swift"
      provides: "Pure HUD projection and generation-safe visibility transitions"
      contains: "HUDPresentationState"
    - path: "Exmen/Views/ExecutionHUDWindow.swift"
      provides: "Click-through, non-activating overlay lifecycle driven by ActionExecutor"
      contains: "ExecutionHUDWindowController"
    - path: "Exmen/Views/ExecutionHUDView.swift"
      provides: "Running, determinate progress, success, and failure presentation"
      contains: "ExecutionHUDView"
  key_links:
    - from: "Exmen/Views/ExecutionHUDWindow.swift"
      to: "Exmen/Services/ActionExecutor.swift"
      via: "published ExecutionSession drives show/update/delayed hide"
      pattern: "ActionExecutor.shared"
    - from: "Exmen/Views/ExecutionHUDView.swift"
      to: "Exmen/Models/ExecutionSession.swift"
      via: "script/orchestration progress and terminal status rendering"
      pattern: "ExecutionSession"
---

<objective>
Complete Phase 12 with a live, focus-safe execution HUD and prove the global summon flow end to end.

Purpose: make action progress visible after the palette disappears while preserving zero-friction use of the previously frontmost application.
Output: tested HUD state machine, overlay window/view, startup wiring, integration coverage, and manual acceptance evidence.
</objective>

<execution_context>
@.planning/phases/12-global-summon-palette-hud/12-RESEARCH.md
@.planning/phases/12-global-summon-palette-hud/12-01-SUMMARY.md
@.planning/phases/12-global-summon-palette-hud/12-02-SUMMARY.md
@.planning/phases/12-global-summon-palette-hud/12-03-SUMMARY.md
@Exmen/Services/ActionExecutor.swift
@Exmen/Services/SubtaskOrchestrator.swift
@Exmen/Views/SubtaskProgressWindow.swift
@Exmen/ExmenApp.swift
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Define and test HUD projection/visibility state</name>
  <files>Exmen/Models/HUDPresentationState.swift, Tests/ExmenTests/HUDPresentationStateTests.swift, Tests/ExmenTests/SummonIntegrationTests.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Create a pure HUD presentation model projected from ExecutionSession: hidden, running-indeterminate, running-determinate(progress/detail), succeeded, and failed(message). Define explicit completion dwell durations (short success, longer failure) and a monotonically increasing run generation/token.

Test script and orchestration projections, 0/100% boundaries, failure summaries, new-run-before-old-hide, duplicate terminal updates, and hide-token mismatch. Add narrow integration tests using injected executor dependencies to prove menu-equivalent and palette-equivalent launch calls publish identical session/HUD sequences and busy rejection does not create a second HUD generation. Do not make unit tests depend on real wall-clock sleeps.
  </action>
  <verify>
`xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS' -only-testing:ExmenTests/HUDPresentationStateTests -only-testing:ExmenTests/SummonIntegrationTests`
  </verify>
  <done>The complete HUD lifecycle, including stale-timer protection, is deterministic under tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the non-activating HUD window and SwiftUI view</name>
  <files>Exmen/Views/ExecutionHUDView.swift, Exmen/Views/ExecutionHUDWindow.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Build a compact visual surface with action icon/name, spinner for scripts, determinate bar and percentage/detail for orchestrations, and distinct success/failure terminal styling. Respect Reduce Motion and accessibility labels; avoid relying on color alone.

Host it in a borderless transparent NSPanel/NSWindow that cannot become key, ignores mouse events, joins all spaces/full-screen auxiliary behavior where supported, and never calls `NSApp.activate`. Place it within the active screen's visible frame (consistent corner/edge with safe margins). Observe ActionExecutor on the main actor, show/update immediately, schedule terminal hide using cancellable Task/DispatchWorkItem plus run generation validation, and order out rather than destroy/recreate on every update. Remove observers/cancellables on teardown.
  </action>
  <verify>
`xcodebuild -project Exmen.xcodeproj -scheme Exmen -configuration Debug build`
  </verify>
  <done>The HUD renders all session states, remains click-through/focus-safe, and cannot be hidden by stale completion work.</done>
</task>

<task type="auto">
  <name>Task 3: Wire startup and run full automated integration checks</name>
  <files>Exmen/ExmenApp.swift, Tests/ExmenTests/SummonIntegrationTests.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Instantiate/retain the HUD controller for app lifetime during Exmen startup so menu and palette runs are observed from first launch. Ensure app termination unregisters the hot key and cancels HUD/palette observers without disturbing existing ServiceManager cleanup. Add assertions for malformed actions, failed scripts, failed orchestration summary, empty palette, and rapid completion/new-run sequencing.

Run the entire test target and a clean Debug build. Fix warnings/errors introduced by Phase 12; do not suppress concurrency or sendability warnings without understanding them.
  </action>
  <verify>
`xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS' && xcodebuild -project Exmen.xcodeproj -scheme Exmen -configuration Debug build`
  </verify>
  <done>All automated Phase 12 behavior and the existing suite pass in the production app target.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify global summon and HUD behavior on macOS</name>
  <files>None (verification only)</files>
  <action>
Launch the freshly built Exmen.app and perform the checks below with at least one ordinary script action and one multi-subtask action. Record observed results in the plan summary. Any failure blocks phase closeout and must be fixed/retested rather than documented as complete.
  </action>
  <verify>
1. With another app frontmost, default Option-Space opens one centered palette on the active display.
2. Type to filter, use arrows, Enter to launch, and Escape to dismiss; focus returns to the prior app.
3. Open Settings, record a new shortcut, relaunch Exmen, and confirm only the new shortcut works.
4. Attempt an occupied/invalid shortcut and confirm the prior valid shortcut remains active with visible feedback.
5. Launch a normal script from the menu and from the palette; both show indeterminate HUD then terminal state and auto-hide.
6. Launch an orchestration; HUD percentage/detail updates live and failure styling appears if one subtask fails.
7. Start a new action as soon as a prior one completes; the prior hide timer does not hide the new HUD.
8. Confirm HUD does not steal clicks/typing and palette/HUD behave across Spaces/full-screen and multiple displays if available.
  </verify>
  <done>User accepts global shortcut configuration, palette keyboard UX, live HUD behavior, focus safety, and automatic dismissal.</done>
</task>

</tasks>

<verification>
1. `xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS'`
2. `xcodebuild -project Exmen.xcodeproj -scheme Exmen -configuration Debug build`
3. All SUMMON-01...SUMMON-05 requirements map to passing tests plus Task 4 manual evidence.
4. No new Accessibility permission is requested and no third-party shortcut dependency was added.
</verification>

<success_criteria>
- Global summon works from other applications with a durable configurable shortcut.
- Palette search/navigation launches the intended action exactly once.
- Menu and palette share execution state and HUD behavior.
- HUD reports script/orchestration progress, preserves focus, and auto-hides safely.
- Full tests, Debug build, and blocking human smoke test pass before Phase 12 closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/12-global-summon-palette-hud/12-04-SUMMARY.md` and `.planning/phases/12-global-summon-palette-hud/12-VERIFICATION.md`.
</output>
