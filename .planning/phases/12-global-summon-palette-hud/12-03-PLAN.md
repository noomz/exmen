---
phase: 12-global-summon-palette-hud
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - Exmen/Models/PaletteSearch.swift
  - Exmen/Views/CommandPaletteView.swift
  - Exmen/Views/CommandPaletteWindow.swift
  - Exmen/ExmenApp.swift
  - Tests/ExmenTests/PaletteSearchTests.swift
  - Tests/ExmenTests/CommandPaletteStateTests.swift
  - Exmen.xcodeproj/project.pbxproj
autonomous: true
requirements: [SUMMON-01, SUMMON-03, SUMMON-04]
must_haves:
  truths:
    - "The registered shortcut toggles one centered Spotlight-style palette on the active display"
    - "Typing filters runnable actions case/diacritic-insensitively with name matches ranked before description-only matches"
    - "Up/Down moves a clamped selection, Enter launches exactly one selected action, and Escape dismisses"
    - "The palette updates when ActionService reloads and handles zero matches without stale selection or crashes"
    - "Showing/dismissing the palette does not leave Exmen permanently frontmost or create duplicate panels"
  artifacts:
    - path: "Exmen/Models/PaletteSearch.swift"
      provides: "Pure filtering, ranking, and selection-clamping logic"
      contains: "PaletteSearch"
    - path: "Exmen/Views/CommandPaletteWindow.swift"
      provides: "Reusable key-capable NSPanel lifecycle and summon toggle"
      contains: "CommandPaletteWindowController"
    - path: "Exmen/Views/CommandPaletteView.swift"
      provides: "Search field, result rows, empty state, and keyboard command handling"
      contains: "CommandPaletteView"
  key_links:
    - from: "Exmen/Services/GlobalHotKeyManager.swift"
      to: "Exmen/Views/CommandPaletteWindow.swift"
      via: "main-actor summon callback toggles shared window controller"
      pattern: "CommandPaletteWindowController.shared.toggle"
    - from: "Exmen/Views/CommandPaletteView.swift"
      to: "Exmen/Services/ActionExecutor.swift"
      via: "Enter/click executes selected action through shared path"
      pattern: "ActionExecutor.shared"
---

<objective>
Deliver the keyboard-first Spotlight-style command palette invoked by the global shortcut.

Purpose: let users find and run an action from any app with no menu traversal.
Output: deterministic search/state logic, key-capable floating panel, SwiftUI palette, summon wiring, and behavioral tests.
</objective>

<execution_context>
@.planning/phases/12-global-summon-palette-hud/12-RESEARCH.md
@.planning/phases/12-global-summon-palette-hud/12-01-SUMMARY.md
@.planning/phases/12-global-summon-palette-hud/12-02-SUMMARY.md
@Exmen/Services/ActionService.swift
@Exmen/Services/ActionExecutor.swift
@Exmen/Models/Action.swift
@Exmen/Views/SubtaskProgressWindow.swift
@Exmen/Views/ServiceOutputWindow.swift
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic action filtering and selection state</name>
  <files>Exmen/Models/PaletteSearch.swift, Tests/ExmenTests/PaletteSearchTests.swift, Tests/ExmenTests/CommandPaletteStateTests.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Implement pure search over non-service actions. Empty query preserves ActionService order. Normalize case and diacritics. Rank exact/prefix name matches first, then name token/substring matches, then description-only matches; preserve source order for equal scores. Do not use UUID order or unstable dictionary iteration.

Add a small palette state model responsible for query, filtered results, and selected index/id. It must reset predictably on show, clamp after query/action-list changes, wrap or stop at boundaries according to one documented behavior, no-op Enter when empty, and return the selected Action exactly once. Test empty lists, no matches, accents/case, ranking, stable ties, service exclusion, list reloads, and arrow boundaries.
  </action>
  <verify>
`xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS' -only-testing:ExmenTests/PaletteSearchTests -only-testing:ExmenTests/CommandPaletteStateTests`
  </verify>
  <done>All palette behavior that can be tested without a window is deterministic and edge-case covered.</done>
</task>

<task type="auto">
  <name>Task 2: Build the reusable command-palette panel</name>
  <files>Exmen/Views/CommandPaletteWindow.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Create an NSPanel subclass/controller following existing independent-window patterns, but configured as a borderless/transient Spotlight-style panel that can become key. Host SwiftUI via NSHostingController, keep exactly one shared panel, and make repeated summon calls toggle it.

Before activation capture `NSWorkspace.shared.frontmostApplication`. Position on the screen containing the active app's key/main window when available, otherwise the mouse/main screen, using visibleFrame so menu bar/Dock are respected. Activate Exmen only to receive palette input. On Escape, outside/deactivation dismissal, or successful launch, order out and restore the previously frontmost app where safe. Remove local/global event monitors on close/deinit. Do not let panel lifecycle terminate the LSUIElement app.
  </action>
  <verify>
`xcodebuild -project Exmen.xcodeproj -scheme Exmen -configuration Debug build`
  </verify>
  <done>One leak-free, key-capable palette panel can show, toggle, center, and dismiss without persistent focus theft.</done>
</task>

<task type="auto">
  <name>Task 3: Build SwiftUI palette and connect hotkey to execution</name>
  <files>Exmen/Views/CommandPaletteView.swift, Exmen/Views/CommandPaletteWindow.swift, Exmen/ExmenApp.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Build the search field and result list with action icon, display title, and description/status. Autofocus search on every show. Handle Up/Down before the TextField consumes them, Enter to execute the selected action via ActionExecutor.shared, and Escape to dismiss. Mouse click launches through the same method. Disable launch while ActionExecutor reports busy and provide a concise running-state hint instead of silently dropping input.

Observe ActionService.shared so reloads update results. Show accessible empty/no-match states and expose labels/selection semantics for VoiceOver. Replace Plan 12-02's temporary summon hook with a direct main-actor toggle of CommandPaletteWindowController.shared. Reset query and selection on each fresh show, dismiss after accepted execution, and do not route service actions through ScriptRunner.
  </action>
  <verify>
`xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS'`
  </verify>
  <done>The global shortcut opens a searchable, fully keyboard-operable palette and selected actions use the shared executor exactly once.</done>
</task>

</tasks>

<verification>
1. `xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS'`
2. `xcodebuild -project Exmen.xcodeproj -scheme Exmen -configuration Debug build`
3. Manual happy path: summon from TextEdit, type a partial action name, move selection, press Enter, and confirm the intended action runs once.
4. Manual failure paths: no matches + Enter, empty action list, repeated shortcut toggling, and Escape all behave without crash/focus leak.
5. Manual placement: summon once on each connected display and confirm the panel uses the active display's visible frame.
</verification>

<success_criteria>
- SUMMON-01 opens the production palette, not a placeholder callback.
- Search and ranking are deterministic and tested.
- Keyboard navigation and empty/reload boundaries are safe.
- All action launches flow through ActionExecutor.
- Panel focus and lifecycle survive repeated summon/dismiss cycles.
</success_criteria>

<output>
After completion, create `.planning/phases/12-global-summon-palette-hud/12-03-SUMMARY.md`.
</output>
