---
phase: 12-global-summon-palette-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Exmen/Models/ExecutionSession.swift
  - Exmen/Services/ActionExecutor.swift
  - Exmen/Views/MenuContentView.swift
  - Tests/ExmenTests/ExecutionSessionTests.swift
  - Tests/ExmenTests/ActionExecutorTests.swift
  - Exmen.xcodeproj/project.pbxproj
autonomous: true
requirements: [SUMMON-03, SUMMON-05]
must_haves:
  truths:
    - "Menu and future palette launches use one shared action-execution path"
    - "A running action publishes action identity, status, elapsed start time, detail, and optional determinate progress"
    - "Single scripts and orchestrated actions both reach terminal success/failure states without losing existing output delivery"
    - "A second launch while another action is running is rejected deterministically instead of corrupting HUD state"
    - "Menu rows reflect shared execution state rather than private view-only state"
  artifacts:
    - path: "Exmen/Models/ExecutionSession.swift"
      provides: "Equatable execution lifecycle model consumed by menu, palette, and HUD"
      contains: "ExecutionSession"
    - path: "Exmen/Services/ActionExecutor.swift"
      provides: "Observable shared service routing script and orchestration actions"
      contains: "ActionExecutor"
    - path: "Tests/ExmenTests/ActionExecutorTests.swift"
      provides: "Busy rejection and script/orchestration lifecycle coverage"
      contains: "ActionExecutorTests"
  key_links:
    - from: "Exmen/Views/MenuContentView.swift"
      to: "Exmen/Services/ActionExecutor.swift"
      via: "execute(action:) and published session/output state"
      pattern: "ActionExecutor.shared"
    - from: "Exmen/Services/ActionExecutor.swift"
      to: "Exmen/Services/SubtaskOrchestrator.swift"
      via: "mirrors overallProgressPercent/completionSummary during orchestrated runs"
      pattern: "SubtaskOrchestrator.shared"
---

<objective>
Centralize action execution and expose one observable lifecycle before adding a second launcher or HUD.

Purpose: eliminate duplicated script/orchestration logic and give all Phase 12 surfaces a consistent, testable source of truth.
Output: execution session model, shared executor, menu migration, and focused lifecycle tests.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-global-summon-palette-hud/12-RESEARCH.md
@Exmen/Views/MenuContentView.swift
@Exmen/Services/ScriptRunner.swift
@Exmen/Services/SubtaskOrchestrator.swift
@Exmen/Models/SubtaskState.swift
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Define execution session semantics and test seams</name>
  <files>Exmen/Models/ExecutionSession.swift, Tests/ExmenTests/ExecutionSessionTests.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Create an Equatable execution model with a stable run id, action id/name, start date, kind (`script` or `orchestration`), lifecycle (`running`, `succeeded`, `failed`), optional progress constrained to 0...1, human-readable detail, and terminal result metadata needed by existing popup/notification delivery.

Keep AppKit/SwiftUI out of the model. Add tests for initial running state, progress clamping/normalization, terminal transitions, and failure detail. Make illegal combinations unrepresentable where practical (for example terminal states must not remain indeterminate-running).
  </action>
  <verify>
`xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS' -only-testing:ExmenTests/ExecutionSessionTests`
  </verify>
  <done>Execution lifecycle semantics are independently testable and suitable for menu, palette, and HUD observation.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared ActionExecutor with busy and failure handling</name>
  <files>Exmen/Services/ActionExecutor.swift, Tests/ExmenTests/ActionExecutorTests.swift, Exmen.xcodeproj/project.pbxproj</files>
  <action>
Implement `@MainActor final class ActionExecutor: ObservableObject` with `shared`, a published current/latest session, and a published popup payload compatible with the existing menu result view. Route ordinary actions through ScriptRunner and orchestration actions through SubtaskOrchestrator.shared. Preserve HookParser cleanup, OutputService handling, orchestration summary notifications, and popup behavior from MenuContentView.

Provide internal initializer dependencies/protocol adapters for tests rather than sleeping or launching real shell commands. Return an explicit busy outcome when a run is active. Treat missing script configuration, runner errors, and orchestration validation errors as terminal failures and clear the busy state; no path may strand a session in `.running`. Mirror orchestration progress and active subtask detail onto the session on the main actor. Do not change SubtaskOrchestrator's class-level actor isolation.
  </action>
  <verify>
`xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS' -only-testing:ExmenTests/ActionExecutorTests`
  </verify>
  <done>One observable service owns both execution paths, publishes progress/results, rejects overlap, and is covered without real processes.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate menu execution to ActionExecutor</name>
  <files>Exmen/Views/MenuContentView.swift</files>
  <action>
Replace private `executingActionId`, `executeAction`, `executeSubtaskAction`, and summary-delivery ownership with observation of ActionExecutor.shared. Menu rows derive their spinner/disabled state from the shared running session. Row clicks call the executor exactly once. Bind existing PopupResultView presentation to the executor's delivered popup payload and keep refresh, service rows, hide-on-click, and quit behavior unchanged.

Do not add palette or HUD UI in this plan. This refactor is the integration proof that the executor is production-capable before other launch surfaces depend on it.
  </action>
  <verify>
`xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS'`
  </verify>
  <done>The menu compiles and all existing tests pass while using the same execution source future Phase 12 surfaces will consume.</done>
</task>

</tasks>

<verification>
1. `xcodebuild test -project Exmen.xcodeproj -scheme Exmen -destination 'platform=macOS'`
2. Confirm `rg -n 'private func executeAction|private func executeSubtaskAction' Exmen/Views/MenuContentView.swift` returns no matches.
3. Confirm failure and busy-path tests assert that no session remains permanently running.
</verification>

<success_criteria>
- There is exactly one production action-execution service.
- Both script and orchestration progress are represented by one session model.
- Existing menu output behavior remains intact.
- Busy, malformed-action, runner-failure, and orchestration-failure paths are tested.
- Full ExmenTests suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-global-summon-palette-hud/12-01-SUMMARY.md`.
</output>
