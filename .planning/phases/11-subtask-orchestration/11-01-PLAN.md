---
phase: 11-subtask-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Exmen.xcodeproj/project.pbxproj
  - Exmen.xcodeproj/xcshareddata/xcschemes/Exmen.xcscheme
  - Tests/ExmenTests/SubtaskConfigTests.swift
  - Tests/ExmenTests/WaveSchedulerTests.swift
  - Tests/ExmenTests/SubtaskStateTests.swift
  - Tests/ExmenTests/DynamicSpawnTests.swift
  - Tests/ExmenTests/SummaryTests.swift
  - Tests/ExmenTests/CascadeSkipTests.swift
  - Tests/ExmenTests/ConcurrencyCapTests.swift
  - Tests/ExmenTests/TimeoutTests.swift
  - Tests/ExmenTests/ProgressModelTests.swift
autonomous: false
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04, ORCH-05, ORCH-06]
must_haves:
  truths:
    - "xcodebuild can run the ExmenTests target and report pass/fail counts"
    - "Every ORCH requirement has at least one XCTest file that fails for the right reason (no production code yet)"
  artifacts:
    - path: "Tests/ExmenTests/SubtaskConfigTests.swift"
      provides: "Wave 0 red tests for ORCH-01 TOML decode"
    - path: "Exmen.xcodeproj/xcshareddata/xcschemes/Exmen.xcscheme"
      provides: "Shared scheme wiring the test target so xcodebuild test works"
  key_links:
    - from: "ExmenTests target"
      to: "Exmen app target"
      via: "@testable import Exmen + host application / test target dependency"
      pattern: "@testable import Exmen"
---

<objective>
Establish the test infrastructure required by the Nyquist validation contract (11-VALIDATION.md). The project has NO test target today — all current validation is manual. This plan adds an `ExmenTests` XCTest target to the Xcode project, a shared `Exmen` scheme that runs it, and one red (failing/compiling-against-stub) test file per requirement suite so subsequent waves have an automated feedback loop.

Covers D-01 (`[[subtasks]]` array fixtures) and D-04 (run mode implied by depends_on — exercised by WaveSchedulerTests) at the test-contract level, plus D-13/D-14 cascade-skip fixtures.

Purpose: Without a test target, no ORCH-01..06 task can be verified automatically. This is the prerequisite gate for every downstream plan.
Output: A buildable `ExmenTests` target, a shared scheme, and 9 XCTest files mapping every requirement to a concrete `xcodebuild test -only-testing:...` command.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-subtask-orchestration/11-VALIDATION.md
@.planning/phases/11-subtask-orchestration/11-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols REFERENCED by Wave 0 tests but CREATED in later plans (declare here so drift checks
treat them as new, not missing): `SubtaskConfig`, `SubtaskConfig.resolvedName`,
`SubtaskConfig.resolvedScript`, `SubtaskConfig.resolvedTimeout`, `SubtaskConfig.decodeInlineTable(_:)`,
`SubtaskStatus` (enum: `pending`/`running`/`succeeded`/`failed(exitCode:)`/`skipped`),
`SubtaskStatus.dotColor`, `SubtaskStatus.isTerminal`, `SubtaskState` (struct),
`SubtaskState.elapsed`, `ActionConfig.subtasks`, `SubtaskOrchestrator`,
`SubtaskOrchestrator.computeWaves(_:)`, `SubtaskOrchestrator.defaultConcurrencyLimit`,
`SubtaskConfig.defaultTimeout`, `OrchestratorError` (cases:
`unknownDependency`/`dependencyCycle`/`maxSubtaskCountExceeded`), `OrchestrationSummary`,
`SubtaskRunner`, `SubtaskEvent`, `HookParser.parseLine(_:)`, `HookLineEvent`.

These tests are EXPECTED to fail or not-compile-link against missing symbols until the
referenced plan lands. Wave 0's done-state is: the test target builds, the scheme runs it,
and each suite fails for the intended (missing-implementation) reason — NOT a green suite.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add ExmenTests XCTest target and shared scheme to the Xcode project</name>
  <files>Exmen.xcodeproj/project.pbxproj, Exmen.xcodeproj/xcshareddata/xcschemes/Exmen.xcscheme</files>
  <read_first>
    - Exmen.xcodeproj/project.pbxproj (the file being modified — note objectVersion 56, hand-maintained sequential IDs of the form A100000X233456780000NNN, the single `Exmen` PBXNativeTarget at A1000006233456780000001, productType com.apple.product-type.application, and the Sources/Frameworks/Resources build phases)
    - Exmen.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved (confirms TOMLDecoder + SwiftTerm are the only SPM deps)
    - .planning/phases/11-subtask-orchestration/11-VALIDATION.md (Wave 0 Requirements list + test-command shapes)
  </read_first>
  <action>
    Add a new `PBXNativeTarget` named `ExmenTests` with productType `com.apple.product-type.unit-test-bundle`, a unit-test-bundle product reference `ExmenTests.xctest`, and a `PBXTargetDependency` on the `Exmen` app target (host application = Exmen). Editing the hand-maintained pbxproj by hand is error-prone; prefer driving the edit with the `xcodeproj` Ruby gem if available (`gem list -i xcodeproj`) via a one-off Ruby script that opens the project, creates the target with `new_target(:unit_test_bundle, 'ExmenTests', :osx)`, adds the `Tests/ExmenTests/*.swift` files, sets `TEST_HOST`/`BUNDLE_LOADER` to the Exmen app, and saves. If the gem is unavailable, hand-edit pbxproj following the existing ID convention: add PBXBuildFile + PBXFileReference entries for each test file, a new PBXSourcesBuildPhase, a PBXGroup `ExmenTests`, the PBXNativeTarget, a PBXTargetDependency + PBXContainerItemProxy, and XCBuildConfiguration entries (Debug/Release) with `GENERATE_INFOPLIST_FILE = YES`, `SWIFT_VERSION = 6.0`, `PRODUCT_BUNDLE_IDENTIFIER = com.exmen.ExmenTests`, `TEST_HOST = $(BUILT_PRODUCTS_DIR)/Exmen.app/Contents/MacOS/Exmen`, `BUNDLE_LOADER = $(TEST_HOST)`. Then create a shared scheme at `Exmen.xcodeproj/xcshareddata/xcschemes/Exmen.xcscheme` whose `TestAction` references the `ExmenTests` BuildableReference (so `xcodebuild test -scheme Exmen` discovers it). Use `@testable import Exmen` in tests, which requires the app target to build with `ENABLE_TESTABILITY = YES` for Debug (verify/add).
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme Exmen -destination 'platform=macOS' 2>&1 | tail -5; xcodebuild -list -project Exmen.xcodeproj 2>&1 | grep -q ExmenTests && echo "TARGET_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -list -project Exmen.xcodeproj` output contains `ExmenTests`.
    - File `Exmen.xcodeproj/xcshareddata/xcschemes/Exmen.xcscheme` exists and its `TestAction` block names `ExmenTests`.
    - `xcodebuild build-for-testing -scheme Exmen -destination 'platform=macOS'` compiles the app target (test files may not yet exist — Task 2 adds them; this task may be co-verified after Task 2).
  </acceptance_criteria>
  <done>The Xcode project exposes an `ExmenTests` unit-test target reachable via `xcodebuild test -scheme Exmen`, with a committed shared scheme.</done>
</task>

<task type="auto">
  <name>Task 2: Author red XCTest suites for every ORCH requirement</name>
  <files>Tests/ExmenTests/SubtaskConfigTests.swift, Tests/ExmenTests/WaveSchedulerTests.swift, Tests/ExmenTests/SubtaskStateTests.swift, Tests/ExmenTests/DynamicSpawnTests.swift, Tests/ExmenTests/SummaryTests.swift, Tests/ExmenTests/CascadeSkipTests.swift, Tests/ExmenTests/ConcurrencyCapTests.swift, Tests/ExmenTests/TimeoutTests.swift, Tests/ExmenTests/ProgressModelTests.swift</files>
  <read_first>
    - .planning/phases/11-subtask-orchestration/11-VALIDATION.md (Per-Task Verification Map — the exact behaviors each suite must assert)
    - .planning/phases/11-subtask-orchestration/11-RESEARCH.md (Research Targets 3, 4, 6 + Code Examples — signatures the tests assert against: `SubtaskConfig`, `computeWaves`, `decodeInlineTable`, `SubtaskStatus`, `OrchestratorError`)
    - Exmen/Models/ActionConfig.swift (ScriptConfig CodingKeys the TOML fixtures must match)
  </read_first>
  <action>
    Create one XCTest case file per suite under `Tests/ExmenTests/`, each starting with `import XCTest` and `@testable import Exmen`. Map suites to the VALIDATION contract: `SubtaskConfigTests` asserts a `[[subtasks]]` TOML string decodes into `[SubtaskConfig]` with required `id` + embedded `ScriptConfig` (D-01), and that `resolvedName`/`resolvedTimeout` defaults apply (ORCH-01). `WaveSchedulerTests` asserts `computeWaves` produces correct wave ordering for a diamond graph where no-dependency subtasks share wave 0 (D-04 run-mode-implied-by-depends_on) and throws `OrchestratorError.dependencyCycle` naming offending ids for a cycle (ORCH-02). `SubtaskStateTests` asserts `SubtaskStatus` cases, `dotColor` mapping, `isTerminal`, and pending→running→succeeded/failed/skipped legality (ORCH-03). `DynamicSpawnTests` asserts `SubtaskConfig.decodeInlineTable` parses `{ id = "build", cmd = "make", depends_on = ["fetch"] }` into a config whose `resolvedScript` is inline (D-06), that duplicate-id spawn is ignored idempotently (D-08), that an unknown `depends_on` errors only that subtask (D-07), and that `EXMEN:progress=` parses + clamps to 0–100 via `HookParser.parseLine` (D-12) (ORCH-04). `SummaryTests` asserts `OrchestrationSummary` counts `N succeeded / M failed / K skipped` and verdict = failed when ≥1 failed (D-15) (ORCH-05). `CascadeSkipTests` asserts a failed subtask marks transitive dependents `skipped` while independent branches still run (D-13/D-14) (ORCH-05). `ConcurrencyCapTests` asserts no more than the cap run simultaneously (ORCH-06). `TimeoutTests` asserts a subtask exceeding its timeout transitions to `failed`/timed-out and its process is dead after the grace period (ORCH-06). `ProgressModelTests` asserts the `@Published` model emits updates during a run (ORCH-03). For symbols not yet implemented, write the assertions referencing the real (future) symbol names so the suite fails to link/compile until the implementing plan lands — this is the intended red state. Mark long-running integration tests (`ConcurrencyCapTests`, `TimeoutTests`, `ProgressModelTests`) with short fixtures (sub-2s sleeps) to keep feedback under 120s.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Exmen -destination 'platform=macOS' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files exist under `Tests/ExmenTests/` and each contains `@testable import Exmen`.
    - Each requirement ORCH-01..06 is covered by at least one `func test...` method (grep each file for `func test`).
    - Running the suite produces failures/build errors that reference the missing production symbols (red state), NOT a passing suite — confirming the tests exercise real (future) APIs.
  </acceptance_criteria>
  <done>Every ORCH requirement maps to a concrete `xcodebuild test -only-testing:ExmenTests/<Suite>` command that currently fails for the right reason.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm test target is discovered and runs (red expected)</name>
  <action>Pause for the developer to confirm the ExmenTests target and shared scheme are wired correctly before downstream waves rely on the automated feedback loop. No code changes in this task.</action>
  <what-built>An `ExmenTests` XCTest target, a shared `Exmen` scheme that runs it, and 9 red test suites covering ORCH-01..06.</what-built>
  <how-to-verify>
    1. Run `xcodebuild -list -project Exmen.xcodeproj` and confirm `ExmenTests` appears under Targets and the `Exmen` scheme is listed.
    2. Run `xcodebuild test -scheme Exmen -destination 'platform=macOS'` and confirm the test bundle is discovered and executes (failures expected — production code not written yet).
    3. Confirm the failures are "cannot find 'SubtaskConfig'/'computeWaves'/etc." or assertion failures against stubs — NOT "no tests found" or scheme/target misconfiguration.
  </how-to-verify>
  <resume-signal>Type "approved" once the test target is discovered and runs, or describe the configuration issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → decoder | Test TOML strings exercise the same decode path users' configs will; ensures malformed input is caught by tests, not in production only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | pbxproj hand-edit corrupting the app target | mitigate | Verify app target still builds (`build-for-testing`) before/after edit; prefer `xcodeproj` gem over manual edit to avoid ID collisions |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase; RESEARCH Package Legitimacy Audit confirms zero new dependencies. The optional `xcodeproj` Ruby gem is a local dev-only tool, not shipped. |
</threat_model>

<verification>
- `xcodebuild -list -project Exmen.xcodeproj` shows `ExmenTests`.
- `xcodebuild test -scheme Exmen -destination 'platform=macOS'` runs the bundle (red expected).
- 9 test files present with `@testable import Exmen`.
</verification>

<success_criteria>
- ExmenTests target builds and is runnable via `xcodebuild test`.
- All ORCH-01..06 requirements have a mapped, currently-red test suite.
- Shared scheme committed so downstream waves get an automated feedback loop.
</success_criteria>

<output>
Create `.planning/phases/11-subtask-orchestration/11-01-SUMMARY.md` when done
</output>
